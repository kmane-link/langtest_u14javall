# Language setting
language: java
services:
  - cassandra 
  - couchdb 
 # - elasticsearch 
  - memcached 
  - mongodb 
  - mysql 
  - neo4j 
  - postgres 
  - rabbitmq 
  - redis 
  - rethinkdb 
  - riak 
  - selenium 
  - sqllite 

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  - oraclejdk8
  - openjdk8

build:
  pre_ci_boot:
    image_name: drydock/u14javall
    image_tag: master
    pull: true

  ci:
    - java -version
   # - ant -version
    - gradle --version
    - mvn --version    
    
